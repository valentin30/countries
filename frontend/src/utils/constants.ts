export const MIN_TIME_FOR_DOWNLOAD = 2000
export const INIT_PAGE = 1
export const INIT_SIZE = 20
export const PAGE_SIZES = [5, 10, 20, 30, 40, 50]

export enum TableColumns {
    CODE = 'code',
    FLAG = 'flag',
    NAME = 'name',
    CAPITAL = 'capital',
    POPULATION = 'population',
    REGION = 'region',
    SUBREGION = 'subregion'
}

export const TableColumsArray = [
    TableColumns.CODE,
    TableColumns.FLAG,
    TableColumns.NAME,
    TableColumns.CAPITAL,
    TableColumns.POPULATION,
    TableColumns.REGION,
    TableColumns.SUBREGION
]
